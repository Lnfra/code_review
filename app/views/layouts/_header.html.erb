<!-- Fixed navbar -->
<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "Code Reviewer", root_path, class: "navbar-brand" %>
    </div>
    <div id="navbar" class="navbar-collapse collapse">
      <% if logged_in? %>
        <ul class="nav navbar-nav">
          <li><%= link_to "Questions Index", questions_path %></li>
          <li><%= link_to "Answers Index", answers_path %></li>
        </ul>
      <% end %>
      <ul class="nav navbar-nav navbar-right">
        <% if get_current_user %>
            <li><%= link_to 'Logout', logout_path, method: :delete, data: {confirm: 'Are you Sure?'} %></li>
        <% else %>
            <li><%= link_to 'Login', login_path %></li>
        <% end %>
        </li>

        <% if get_current_user%>
            <li><%= link_to image_tag(get_current_user.avatar.url(:thumb)), get_current_user %></li>
        <% end %>

      </ul>
    </div><!--/.nav-collapse -->
  </div>
</nav>